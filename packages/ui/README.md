# @riftline/ui

Design tokens and accessible React components, developed in Storybook ([ADR-004](../../docs/adr/ADR-004-ui-component-strategy.md)).

Seeded in Phase 1 (tokens + ~6 primitives); hardened in Phase 5. `packages/ui` never imports from `apps/*` or the data packages.
